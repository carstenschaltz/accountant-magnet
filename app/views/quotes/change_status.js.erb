document.querySelector('#close<%= @quote.id %>').click()
document.querySelector("[data-connect-id='<%= @quote.id %>']").remove();
p = document.querySelector("#connect<%= @quote.id %>")
p.innerHTML = "<strong>Connected</strong>";
p.parentElement.setAttribute("id", "connected-pill");
